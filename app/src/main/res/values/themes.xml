<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.LegacyTrainingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textViewStyle">@style/text</item>
        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@color/beer_ipa_yellow</item>
        <item name="statusBarBackground">@color/chouffe_orange</item>
    </style>

    <style name="text">
        <item name="android:fontFamily">@font/lato</item>
    </style>

    <style name="Theme.LegacyTrainingApp" parent="Base.Theme.LegacyTrainingApp" />
</resources>